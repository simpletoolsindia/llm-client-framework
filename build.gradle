plugins {
    id 'java'
    id 'application'
    id 'maven-publish'
    id 'signing'
}

group = 'com.simpletoolsindia'
version = '1.0.0'

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21

    withJavadocJar()
    withSourcesJar()
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.google.code.gson:gson:2.10.1'
    implementation 'org.slf4j:slf4j-api:2.0.16'
    implementation 'org.slf4j:slf4j-simple:2.0.16'
    implementation 'javax.inject:javax.inject:1'

    compileOnly 'org.apache.commons:commons-lang3:3.14.0'
}

application {
    mainClass = 'com.simpletoolsindia.llm.framework.LLMFramework'
}

tasks.named('test') { useJUnit() }

// ============ Publishing Configuration ============

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java

            groupId 'com.simpletoolsindia'
            artifactId 'llm-client-framework'
            version version

            pom {
                name = 'LLM Client Framework'
                description = 'A unified Java framework for interacting with multiple LLM providers'
                url = 'https://github.com/simpletoolsindia/llm-client-framework'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                        distribution = 'repo'
                    }
                }

                developers {
                    developer {
                        id = 'simpletoolsindia'
                        name = 'Simple Tools India'
                        email = 'dev@simpletools.in'
                    }
                }

                scm {
                    connection = 'scm:git@github.com:simpletoolsindia/llm-client-framework.git'
                    developerConnection = 'scm:git@github.com:simpletoolsindia/llm-client-framework.git'
                    url = 'https://github.com/simpletoolsindia/llm-client-framework'
                }

                issueManagement {
                    system = 'GitHub Issues'
                    url = 'https://github.com/simpletoolsindia/llm-client-framework/issues'
                }
            }
        }
    }

    repositories {
        maven {
            name = 'MavenCentral'
            url = 'https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/'
            credentials {
                username = System.getenv('OSSRH_USERNAME') ?: ''
                password = System.getenv('OSSRH_PASSWORD') ?: ''
            }
        }

        maven {
            name = 'Snapshot'
            url = 'https://s01.oss.sonatype.org/content/repositories/snapshots/'
            credentials {
                username = System.getenv('OSSRH_USERNAME') ?: ''
                password = System.getenv('OSSRH_PASSWORD') ?: ''
            }
        }
    }
}

signing {
    sign publishing.publications.mavenJava
}

// Helper tasks
tasks.register('publishToSnapshot') {
    group = 'publishing'
    description = 'Publish snapshot version to Maven Central'
    dependsOn 'publish'
}

tasks.register('publishToRelease') {
    group = 'publishing'
    description = 'Publish release version to Maven Central (requires signing)'
    dependsOn 'publish'
}